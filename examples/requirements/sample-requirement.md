# 示例需求：订单取消

## 背景

用户在订单未完成前可以取消订单。取消后订单不可继续支付，系统需要释放库存并记录取消原因。

## 角色

- 普通用户：只能取消自己的订单。
- 客服：可以帮助用户取消订单。

## 规则

- 待支付订单允许取消。
- 已支付但未发货订单允许申请取消，需要客服审核。
- 已发货订单不允许取消。
- 用户重复提交取消请求时，系统不能重复释放库存。
- 取消成功后需要写入操作日志。

## 接口

前端调用订单取消接口，接口需要返回取消结果和失败原因。
