# 测试用例切片 TP-006

## 运行目录

examples/outputs/runs/sample-requirement-run

## 工作项来源

examples/outputs/runs/sample-requirement-run/process/test-case-work-items.json

## 场景路径

| ID | Title |
|---|---|
| SC-001 | 订单取消业务流程 |
| SC-001-002 | 重复取消与副作用控制 |

## 叶子 SC

SC-001-002

## 测试点

| 字段 | 内容 |
|---|---|
| ID | TP-006 |
| Title | 取消请求幂等与库存副作用 |
| Objective | 验证同一订单重复或并发取消时，系统不会产生重复库存释放或重复取消记录等副作用。 |
| Basis Refs | 来源=examples/requirements/sample-requirement.md；位置=副作用控制；说明=库存不能重复释放。 |
| Test Cases | ID=TC-006；Title=同一订单并发取消只产生一次成功副作用；Preconditions=用户 A 已登录；订单 ORDER_1005 属于用户 A；订单状态为待支付；Test Data=Name=user_id；Value=USER_A；说明=订单所有人；Name=order_id；Value=ORDER_1005；说明=待支付订单；Name=parallel_request_count；Value=2；说明=并发取消请求数量；Steps=Step No=1；Action=对 ORDER_1005 同时发起两次取消请求；Expected=系统处理并发取消请求；Step No=2；Action=查询订单状态、库存释放次数和取消记录数量；Expected=订单最终为取消态，库存只释放一次，成功取消记录不重复；Expected Result=不产生重复业务副作用；不补写未说明的错误码、提示文案或状态值。；Source Refs=来源=examples/requirements/sample-requirement.md；位置=副作用控制；说明=库存不能重复释放。；Level=Level 2 |

## 填写约束

- 只填写 testPoint.testCases[]。
- 不要新增、删除、合并或改写 SC/TP。
- testCases[] 可以暂不填写全局 TC 编号，merge 脚本会统一重排。
- 一个 TC 只覆盖一个可独立执行、独立判定的测试实例。
