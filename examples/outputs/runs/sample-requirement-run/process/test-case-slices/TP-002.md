# 测试用例切片 TP-002

## 运行目录

examples/outputs/runs/sample-requirement-run

## 工作项来源

examples/outputs/runs/sample-requirement-run/process/test-case-work-items.json

## 场景路径

| ID | Title |
|---|---|
| SC-001 | 订单取消业务流程 |
| SC-001-001 | 普通用户取消订单 |

## 叶子 SC

SC-001-001

## 测试点

| 字段 | 内容 |
|---|---|
| ID | TP-002 |
| Title | 本人待支付订单允许取消 |
| Objective | 验证普通用户取消本人名下待支付订单时，系统允许取消并完成状态、库存和取消原因处理。 |
| Basis Refs | 来源=examples/requirements/sample-requirement.md；位置=订单取消规则；说明=待支付本人订单允许取消。 |
| Test Cases | ID=TC-002；Title=本人待支付订单取消被允许；Preconditions=用户 A 已登录；订单 ORDER_1002 属于用户 A；订单状态为待支付；Test Data=Name=user_id；Value=USER_A；说明=订单所有人；Name=order_id；Value=ORDER_1002；说明=本人待支付订单；Name=order_status；Value=PENDING_PAYMENT；说明=待支付状态；Steps=Step No=1；Action=用户 A 对 ORDER_1002 发起取消；Expected=系统接受取消请求；Step No=2；Action=查询 ORDER_1002 的订单状态和库存占用；Expected=订单进入取消态，库存占用释放；Expected Result=取消成功，订单不可继续支付，库存释放并记录取消原因。；Source Refs=来源=examples/requirements/sample-requirement.md；位置=订单取消规则；说明=待支付本人订单允许取消。；Level=Level 2 |

## 填写约束

- 只填写 testPoint.testCases[]。
- 不要新增、删除、合并或改写 SC/TP。
- testCases[] 可以暂不填写全局 TC 编号，merge 脚本会统一重排。
- 生成 TC 前先识别当前 TP 的测试设计因子：输入条件、等价类、边界点、角色权限、业务状态、配置、外部依赖返回、消息顺序、异常类型、接口参数变体、数据组合和预期差异。
- 每个 TP 至少 1 个 TC 只是最低门槛；应生成覆盖该 TP 适用测试设计因子的最小充分 TC 集合。
- 只有输入依据确实只支持一个独立测试实例时，才允许该 TP 下只有 1 个 TC。
- 一个 TC 只覆盖一个可独立执行、独立判定的测试实例。
