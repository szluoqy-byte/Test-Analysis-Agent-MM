# 测试分析方案模板

本模板是 `test-analysis-agent` 的 Markdown 派生阅读版样式参考。主输出事实源是 `deliverables/test-analysis-solution.json`，结构参考 `templates/test-analysis-solution-json-template.json`；本 Markdown 不手工维护，由 `bin/render-run-markdown.py` 渲染。

主输出必须是自包含交付件：后续人工评审或 `@test-design-agent` 只读取本文件，也应能理解需求范围、设计约束、测试场景、测试点、测试点明细和预期结果。

```markdown
# <需求名称> 测试分析方案

## 1. 需求范围

| 字段 | 内容 |
|---|---|
| 需求 ID |  |
| 需求名称 |  |
| 需求来源 |  |
| 设计方案来源 |  |
| 需求摘要 |  |
| 设计方案摘要 |  |
| 本次覆盖范围 |  |
| 本次不覆盖内容 |  |

## 2. 测试场景与测试点

### SC-001 <测试场景名称>

| 字段 | 内容 |
|---|---|
| 场景目标 |  |
| 场景入口/触发方式 |  |
| 执行用户/角色 |  |
| 业务与设计约束 |  |

#### TP-001 E2E场景测试

##### TP-001-001 <端到端主流程测试点明细>

- 测试点详情：<说明该场景端到端业务主流程需要验证什么。>

- 预期结果：<业务主流程按预期完整闭环或待人工分析确认>

#### TP-002 <测试点>

##### TP-002-001 <普通/成功测试点明细>

- 测试点详情：<说明该规则分支、路径分支、状态分支或风险分支需要验证什么。>

- 预期结果：<明确预期结果或待人工分析确认>

##### TP-002-002 <非成功测试点明细>

###### TP-002-002-001 <失败类型明细>

- 测试点详情：<说明该失败类型、异常返回、数据校验、权限控制或外部依赖失败需要验证什么。>

- 预期结果：<明确预期结果或待人工分析确认>
```

## 生成规则

- 主输出 JSON 路径固定为：`${PROJECT_ROOT}/outputs/runs/<run-id>/deliverables/test-analysis-solution.json`；Markdown 路径固定为同目录 `test-analysis-solution.md`，只能由渲染脚本生成。
- 新 run 不应生成自由格式过程分析 Markdown 作为机器证据；review/coverage 结论写入 `reports/*.json`，必要时再渲染同名 Markdown 人读版。
- 运行任务清单、上下文、输入事实模型和澄清记录分别写入 `process/task-list.json`、`process/context-pack.json`、`process/input-fact-model.json` 和 `process/clarification-session.json`，同名 Markdown 由脚本渲染。
- 主交付件固定使用中文术语和缩写：测试场景 `SC-*`、测试点 `TP-*`、测试点明细 `TP-*-*`，不展开英文全名。
- 测试场景 ID 使用 `SC-001` 起连续编号。
- 测试点 ID 使用 `TP-001` 起连续编号；接口、消息、定时任务或批处理内容如果最终形成测试点，也统一使用 `TP-*`。
- 测试点明细 ID 使用所属测试点作为前缀，例如 `TP-001-001`、`TP-001-002`。
- 每个测试场景下必须包含一个 `E2E场景测试` 测试点，用于覆盖该场景端到端业务主流程是否按预期完整闭环。
- `E2E场景测试` 是独立同级测试点，只维护 1 个端到端主流程成功闭环测试点明细；其他规则、异常、接口、权限、状态、回滚或补偿分支必须拆为同级 `TP-*`，不得挂在 `E2E场景测试` 下。
- 当需求、设计方案或用户任务明确要求接口测试/API 契约覆盖时，接口测试或集成覆盖场景下的非 E2E `TP-*` 必须先按接口、端点、消息、回调或集成点组织，例如 `接口：GET /customers/?telephone_exact={phone}`、`接口：POST /customers/{customer_id}/transactions/` 或 `通用接口鉴权规则（适用于所有外部 API）`；不要把“状态码规则”“字段校验”“鉴权规则”这类泛化主题直接作为无法定位目标接口的 `TP-*`。
- 主输出默认按 `测试场景 -> 测试点 -> 测试点明细` 组织；当 `TP-*-*` 测试点明细是明确非成功聚合分支时，必须新增 `TP-*-*-*` 第四层承载失败类型明细。
- 测试点明细必须使用三级标题，不使用表格承载；失败类型明细使用四级标题，不使用表格承载。
- 测试点明细只表达分析层的规则分支、路径分支、状态分支、权限分支、接口契约分支或风险分支；不要展开具体代表性条件、数据、状态组合或边界取值。
- 非成功测试点明细下的第四层用于继续拆分失败类型，按需覆盖业务逻辑、系统交互、外部依赖、异常返回、数据校验、权限控制、状态限制、接口契约和数据一致性等失败来源。
- 只有明确非成功聚合分支才强制新增第四层；成功路径、普通规则分支以及“未找到返回空结果”“列表为空”“count=0”这类单一弱结果分支，仍可停留在 `TP-*-*`。
- `预期结果` 只能写需求或设计方案明确支持的结果；如果需求和设计方案没有明确错误提示、状态变化、错误码、返回内容或记录变化，写 `待人工分析确认`。
- 主输出不设置 `未明确规则` 章节，也不设置独立待确认信息清单。
- 主输出不得使用 Markdown 加粗语法，例如 `**文本**` 或 `__文本__`。
- 主输出不得出现 `TDI-*`、`测试设计项`、`测试设计项 ID` 或测试设计项表格；这些留给 `@test-design-agent`。
- 不要把内部测试技术路由、方法证据、专家评分、quality gate 结果或 memory 更新建议写入主输出。
