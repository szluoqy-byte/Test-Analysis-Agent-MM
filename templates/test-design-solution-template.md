# 测试设计方案模板

本模板是本独立 Agent 的主输出。它不是完整测试用例文档，不包含前置步骤、测试步骤、自动化脚本或执行数据清单。

主输出必须是自包含交付件：后续评审、细化或落地只读取本文件，也应能理解需求范围、设计约束、测试场景、测试点、代表性测试设计项和预期结果。

```markdown
# <需求名称> 测试设计方案

## 1. 需求范围

| 字段 | 内容 |
|---|---|
| 需求 ID |  |
| 需求名称 |  |
| 需求来源 |  |
| 设计方案来源 |  |
| 需求摘要 |  |
| 设计方案摘要 |  |
| 本次覆盖范围 |  |
| 本次不覆盖内容 |  |

## 2. 测试场景与测试设计

### 场景 SC-001：<测试场景名称>

| 字段 | 内容 |
|---|---|
| 场景目标 |  |
| 场景入口/触发方式 |  |
| 执行用户/角色 |  |
| 业务与设计约束 |  |

#### 测试点 TP-001：<测试点>

| 测试设计项 ID | 测试设计项 | 预期结果 |
|---|---|---|
| TD-001 | <代表性条件、数据、状态或组合> | <明确预期结果或待人工分析确认> |
```

## 生成规则

- 主输出路径固定为：`${PROJECT_ROOT}/outputs/runs/<run-id>/deliverables/test-design-solution.md`。
- 如保留过程分析报告，路径固定为 `${PROJECT_ROOT}/outputs/runs/<run-id>/reports/test-analysis-report.md`。
- 运行任务清单、上下文和澄清记录分别写入 `process/task-list.md`、`process/context-pack.md` 和 `process/clarification-session.md`。
- 测试设计项 ID 使用 `TD-001` 起全局连续编号。
- 测试点 ID 使用 `TP-001` 连续递增；接口、消息、定时任务或批处理测试点也统一使用 `TP-*`。
- 主输出必须按 `测试场景 -> 测试点 -> 测试设计项` 组织。
- 测试设计项表头固定为 `测试设计项 ID | 测试设计项 | 预期结果`。
- `测试设计项` 写代表性条件、数据、状态或组合，不写“验证功能正常”这类泛化标题。
- `预期结果` 只能写需求或设计方案明确支持的结果；如果需求和设计方案没有明确错误提示、状态变化、错误码、返回内容或记录变化，写 `待人工分析确认`。
- 主输出不设置 `未明确规则` 章节，也不设置独立待确认信息清单。
- 不要把内部测试技术路由、方法证据、专家评分、quality gate 结果或 memory 更新建议写入主输出。
