# 权限与角色分析 方法参考

本方法参考 用于角色、资源、动作和数据范围相关需求。

## 职责边界

- 本方法参考 只产出内部方法参考记录和权限测试点候选，供 `test-analysis-solution-generation` 归并为场景化测试点。
- 权限矩阵、角色-资源-动作组合和数据范围组合是过程产物，不直接进入最终主交付件。
- 测试分析方案生成阶段会基于权限测试点生成测试点；本方法参考不建议测试用例数量、账号数据或具体操作步骤。

## 输入

- 输入事实模型。
- 角色与权限相关片段。
- `${PROJECT_ROOT}/outputs/runs/<run-id>/process/context-pack.json` 中 `sources[]` 对 `testing-method-router` 可见的项目术语、角色和权限上下文来源。

## 分析步骤

1. 识别角色、资源、动作和数据范围。
2. 构建轻量权限矩阵。
3. 覆盖允许访问、拒绝访问、跨角色访问、跨租户访问和数据隔离。
4. 检查权限变更、权限过期、继承权限和默认权限。
5. 生成权限测试点。
6. 对角色边界、数据归属、默认权限、继承关系和越权处理记录输入不足说明。

## 输出

先输出方法分析证据：

| 证据ID | 方法 | 角色/资源/动作 | 分析结论 | 关联测试点/说明 |
|---|---|---|---|---|

再输出测试点候选：

| 角色 | 资源/数据范围 | 动作 | 测试点 | 需求依据 | 级别 | 风险/备注 |
|---|---|---|---|---|---|---|

如存在权限矩阵缺口，追加：

| 问题ID | checkpoint | sourceStage | header | question | why | impact | options | blockingLevel | priority | askPolicy | mustAsk | relatedRequirement | contextConflict |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|

## 约束

- 没有需求依据时，不假设某角色可访问某资源。
- 角色或数据范围不明确时，标记为 `insufficient_evidence`，不创建问题队列。
- 权限测试点必须同时说明角色、资源或数据范围、动作，避免只写“验证权限正常”。
- 最终主交付件中只保留权限边界和数据范围验证目标，不输出完整权限矩阵或账号级用例数据。
- 不直接向用户提问。
- 如果可见动态来源中的角色权限模型或项目因子不足，只能按 `sources[]` 记录的可见来源或当前需求明确指向的文件补读相关章节，并在方法参考记录中记录来源；不得全目录搜索或全量复制大文件。
